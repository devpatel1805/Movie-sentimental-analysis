# Movie-sentimental-analysis
A machine learning project that classifies IMDB movie reviews as positive or negative using TF-IDF vectorization and a Logistic Regression model. Includes text preprocessing, model training, evaluation, and a script for real-time sentiment prediction on new input, with the model and vectorizer saved via Joblib.
